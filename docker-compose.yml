services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    # Uncomment for development mode
    # environment:
    #   - NODE_ENV=development
    # command: bun run dev
    # volumes:
    #   - .:/app
    #   - /app/node_modules